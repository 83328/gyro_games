[project]
name = "gyro-maze"
version = "0.1.0"
description = "Local multiplayer phone-as-controller demos"
requires-python = ">=3.8"
dependencies = [
    "aiohttp>=3.9.0",
    "cloudflared>=0.0.0",
    "python-dotenv>=1.0.0",
]

[tool.hatch.build.targets.wheel]
packages = []
